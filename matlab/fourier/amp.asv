% Different amplitude taper for window.
samples = 6;
n = 0:samples;
z = zeros(1, 20);
y1 = sin(2*pi*0.5/samples * n);
sig = [z y1 z];
y2 = ones(1);
sig2 = [z y2 z];
plot(y2)

%plot(y1)

plot(abs(fftshift(fft(sig2)))/max(abs(fftshift(fft(sig2))) ))